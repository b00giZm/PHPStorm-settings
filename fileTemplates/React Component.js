var React = require('react');
var cx    = require('classnames');

var ${COMPONENT};
${COMPONENT} = module.exports = React.createClass({
  getInitialState: function() {
    return null;
  },

  render: function() {
    return (
      <div>Hello ${COMPONENT}!</div>
    );
  }
});
